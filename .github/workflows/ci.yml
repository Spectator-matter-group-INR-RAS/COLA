name: CI

on:
  push:
<<<<<<< HEAD
    branches: [ main, master ]
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [ main, master ]
  pull_request_target:
    types: [opened, synchronize, reopened]
    branches: [ main, master ]

jobs:
  ci:
    name: CI (${{ matrix.os }} - ${{ matrix.compiler }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            compiler: g++
          - os: macos-latest
            compiler: clang
=======
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]

jobs:
  build-and-test:
    name: Build and Test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        compiler: [gcc, clang]
        exclude:
          - os: macos-latest
            compiler: gcc
>>>>>>> f669753 (add CMakePresets)

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies (Ubuntu)
        if: matrix.os == 'ubuntu-latest'
        run: |
<<<<<<< HEAD
          wget https://apt.llvm.org/llvm.sh
          chmod +x llvm.sh
          sudo ./llvm.sh 21
          sudo apt-get update
          sudo apt-get install -y build-essential cmake libgtest-dev g++ clang-format-21
          pip install cmake-format
=======
          sudo apt-get update
          sudo apt-get install -y build-essential cmake ninja-build
          sudo apt-get install -y libgtest-dev
>>>>>>> f669753 (add CMakePresets)

      - name: Install dependencies (macOS)
        if: matrix.os == 'macos-latest'
        run: |
<<<<<<< HEAD
          brew install cmake googletest

      - name: Build and Test
        id: build-test
        run: |
          cmake --workflow release-test

      - name: Clang-tidy
        id: clang-tidy
        if: matrix.os == 'ubuntu-latest'
        run: |
          cmake --workflow test

      - name: Check formatting
        id: check-format
        if: always() && matrix.os == 'ubuntu-latest'
        run: |
          cmake --workflow check-format
=======
          brew install cmake ninja googletest

      - name: Setup compiler
        if: matrix.compiler == 'gcc'
        run: |
          sudo apt-get update
          sudo apt-get install -y g++-12
          echo "CC=gcc-12" >> $GITHUB_ENV
          echo "CXX=g++-12" >> $GITHUB_ENV

      - name: Setup compiler (clang)
        if: matrix.compiler == 'clang'
        run: |
          if [ "${{ matrix.os }}" == "ubuntu-latest" ]; then
            sudo apt-get update
            sudo apt-get install -y clang-17
            echo "CC=clang-17" >> $GITHUB_ENV
            echo "CXX=clang++-17" >> $GITHUB_ENV
          else
            echo "CC=clang" >> $GITHUB_ENV
            echo "CXX=clang++" >> $GITHUB_ENV
          fi

      - name: Configure CMake
        run: |
          cmake -B build -S . -G Ninja -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: |
          cmake --build build --config Release

      - name: Configure Tests
        run: |
          cmake -B build-tests -S tests -G Ninja -DCMAKE_BUILD_TYPE=Release

      - name: Build Tests
        run: |
          cmake --build build-tests --config Release

      - name: Run tests
        run: |
          cd build-tests
          ctest --output-on-failure

  clang-format:
    name: Check Code Formatting
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install clang-format
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-format-17

      - name: Check formatting
        run: |
          find . -name "*.cc" -o -name "*.cpp" -o -name "*.cxx" -o -name "*.hh" -o -name "*.hpp" -o -name "*.hxx" -o -name "*.h" | \
          grep -v "./build" | \
          grep -v "./tinyxml2" | \
          xargs clang-format-17 --dry-run --Werror

  cmake-format:
    name: Check CMake Formatting
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install cmake-format
        run: |
          pip3 install cmake-format

      - name: Check CMake formatting
        run: |
          find . -name "CMakeLists.txt" -o -name "*.cmake" | \
          grep -v "./build" | \
          grep -v "./tinyxml2" | \
          xargs cmake-format --check

  clang-tidy:
    name: Static Analysis
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake ninja-build clang-tidy-17 clang-17 libgtest-dev

      - name: Configure CMake
        env:
          CC: clang-17
          CXX: clang++-17
        run: |
          cmake -B build -S . -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_EXPORT_COMPILE_COMMANDS=ON

      - name: Build (generate compile_commands.json)
        run: |
          cmake --build build --config Release

      - name: Run clang-tidy
        run: |
          find . -name "*.cc" -o -name "*.cpp" -o -name "*.cxx" -o -name "*.hh" -o -name "*.hpp" -o -name "*.hxx" -o -name "*.h" | \
          grep -v "./build" | \
          grep -v "./tinyxml2" | \
          xargs clang-tidy-17 -p build

>>>>>>> f669753 (add CMakePresets)
